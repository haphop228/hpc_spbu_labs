# Quick Start Guide - Task 2: Dot Product with OpenMP

## Быстрый старт (Quick Start)

### 1. Компиляция программы (Compile the program)

```bash
cd task2_dot_product_v2
./scripts/compile.sh
```

Скрипт автоматически:
- Определит вашу ОС (macOS/Linux)
- Установит необходимые зависимости (libomp для macOS)
- Скомпилирует программу с оптимизациями
- Проверит работу OpenMP

### 2. Запуск бенчмарков (Run benchmarks)

```bash
./scripts/run_benchmarks.sh
```

Это запустит полный набор тестов:
- Размеры векторов: 10^6, 10^7, 10^8 элементов
- Количество потоков: 1, 2, 4, 8, 16, 32, 64, 128
- Методы: reduction и no-reduction
- По 10 запусков каждой конфигурации

⏱️ **Время выполнения:** ~10-30 минут в зависимости от системы

### 3. Анализ результатов (Analyze results)

```bash
python3 analysis/analyze.py results/benchmark_YYYYMMDD_HHMMSS.csv
```

Замените `YYYYMMDD_HHMMSS` на имя файла, созданного в шаге 2.

Скрипт выведет:
- Таблицу с временем выполнения
- Ускорение (speedup)
- Эффективность (efficiency)
- Сравнение методов

### 4. Построение графиков (Generate graphs)

```bash
python3 analysis/plot_graphs.py
```

Графики будут сохранены в `graphs/`:
- `execution_time_size_*.png` - время выполнения vs потоки
- `speedup_size_*.png` - ускорение vs потоки (с идеальной линией)
- `efficiency_size_*.png` - эффективность vs потоки
- `comparison_methods.png` - сравнение методов
- `summary_table.txt` - сводная таблица

## Быстрый тест (Quick Test)

Для быстрой проверки работы программы:

```bash
# Полный тестовый пайплайн (компиляция + быстрый тест)
./scripts/test_pipeline.sh

# Или ручной тест
./bin/dot_product 1000000 4 reduction 5
./bin/dot_product 1000000 4 no-reduction 5
```

## Структура результатов (Results Structure)

```
results/
├── benchmark_YYYYMMDD_HHMMSS.csv          # Сырые данные
├── benchmark_YYYYMMDD_HHMMSS_processed.csv # Обработанные данные
└── benchmark_YYYYMMDD_HHMMSS_summary.txt   # Краткая сводка

graphs/
├── execution_time_size_1000000.png
├── execution_time_size_10000000.png
├── execution_time_size_100000000.png
├── speedup_size_1000000.png
├── speedup_size_10000000.png
├── speedup_size_100000000.png
├── efficiency_size_1000000.png
├── efficiency_size_10000000.png
├── efficiency_size_100000000.png
├── comparison_methods.png
└── summary_table.txt
```

## Параметры программы (Program Parameters)

```bash
./bin/dot_product <size> <threads> <method> <runs> [output_file]
```

- `size` - размер векторов (количество элементов)
- `threads` - количество потоков OpenMP
- `method` - метод: `reduction` или `no-reduction`
- `runs` - количество запусков для усреднения
- `output_file` - (опционально) файл для сохранения результатов

## Примеры использования (Usage Examples)

```bash
# Базовый тест
./bin/dot_product 1000000 4 reduction 10

# Тест с разными методами
./bin/dot_product 10000000 8 reduction 5
./bin/dot_product 10000000 8 no-reduction 5

# Тест масштабируемости
for threads in 1 2 4 8 16; do
    ./bin/dot_product 10000000 $threads reduction 5
done

# С сохранением в файл
./bin/dot_product 1000000 4 reduction 10 results/my_test.csv
```

## Требования (Requirements)

### Система (System)
- macOS или Linux
- Компилятор с поддержкой OpenMP (gcc или clang+libomp)
- Python 3.6+

### Python библиотеки (Python packages)
```bash
pip3 install pandas matplotlib numpy seaborn
```

## Устранение проблем (Troubleshooting)

### Ошибка компиляции на macOS
```bash
# Установите GCC (рекомендуется)
brew install gcc

# Или libomp для Clang
brew install libomp
```

### Python библиотеки не найдены
```bash
pip3 install --user pandas matplotlib numpy seaborn
```

### Программа работает медленно
- Уменьшите размеры векторов в `scripts/run_benchmarks.sh`
- Уменьшите количество запусков (RUNS=3 вместо 10)
- Используйте меньше потоков

### Недостаточно памяти
- Уменьшите максимальный размер вектора (100000000 → 10000000)
- Закройте другие приложения

## Что дальше? (Next Steps)

1. Изучите сгенерированные графики в `graphs/`
2. Проанализируйте `summary_table.txt`
3. Сравните результаты с редукцией и без
4. Обратите внимание на:
   - Ускорение при увеличении потоков
   - Эффективность параллелизации
   - Разницу между методами
   - Влияние размера задачи

## Для отчета (For Report)

Используйте следующие графики:

1. **Speedup graphs** (`speedup_size_*.png`)
   - Показывают эффективность параллелизации
   - Сравнение с идеальной линией

2. **Comparison graph** (`comparison_methods.png`)
   - Сравнение методов с/без редукции
   - Лучшие конфигурации для каждого размера

3. **Summary table** (`summary_table.txt`)
   - Числовые данные для таблиц в отчете
   - Точные значения ускорения и эффективности

## Ключевые вопросы для анализа

1. Как масштабируется производительность с ростом потоков?
2. Достигается ли линейное ускорение?
3. Какой метод эффективнее и почему?
4. Как размер задачи влияет на эффективность?
5. При каком количестве потоков начинается деградация производительности?

## Типичные результаты (Typical Results)

### Размер 10^6 элементов
- Лучшее ускорение: 4-6x при 4-8 потоках
- Эффективность: 0.6-0.8

### Размер 10^7 элементов
- Лучшее ускорение: 6-8x при 8-16 потоках
- Эффективность: 0.5-0.7

### Размер 10^8 элементов
- Лучшее ускорение: 7-10x при 16-64 потоках
- Эффективность: 0.4-0.6

## Полезные команды (Useful Commands)

```bash
# Просмотр последних результатов
ls -lt results/*.csv | head -5

# Быстрый анализ последнего бенчмарка
python3 analysis/analyze.py $(ls -t results/benchmark_*[0-9].csv | head -1)

# Генерация всех графиков
python3 analysis/plot_graphs.py

# Просмотр сводной таблицы
cat graphs/summary_table.txt

# Очистка результатов
rm -rf results/*.csv graphs/*.png
```

## Контакты и поддержка (Support)

При возникновении проблем:
1. Проверьте раздел "Устранение проблем" выше
2. Убедитесь, что все зависимости установлены
3. Проверьте логи компиляции и выполнения

---

**Удачи в исследовании производительности! 🚀**