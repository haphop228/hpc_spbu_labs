# Quick Start Guide - Task 3: MPI Message Exchange

## Быстрый старт (Quick Start)

### 1. Компиляция программы (Compile the program)

```bash
cd task3_mpi_message_exchange
chmod +x scripts/*.sh
./scripts/compile.sh
```

Скрипт автоматически:
- Определит вашу ОС (macOS/Linux)
- Найдет MPI компилятор (mpic++/mpicxx)
- Скомпилирует программу с оптимизациями
- Проверит работу MPI

### 2. Быстрый тест (Quick Test)

```bash
# Тест с малым сообщением (1 KB)
mpirun -np 2 bin/message_exchange 1024 100

# Тест со средним сообщением (1 MB)
mpirun -np 2 bin/message_exchange 1048576 50

# Тест с большим сообщением (10 MB)
mpirun -np 2 bin/message_exchange 10485760 20
```

### 3. Запуск полного бенчмарка (Run full benchmark)

```bash
./scripts/run_benchmarks.sh
```

Это запустит полный набор тестов:
- Размеры сообщений: от 1 байта до 16 MB
- 11 различных размеров
- 100 итераций для каждого размера
- Автоматическое сохранение результатов

⏱️ **Время выполнения:** ~5-15 минут

### 4. Анализ результатов (Analyze results)

```bash
# Обработка данных (замените YYYYMMDD_HHMMSS на актуальную дату)
python3 analysis/analyze.py results/benchmark_YYYYMMDD_HHMMSS.csv

# Или используйте последний файл автоматически
python3 analysis/analyze.py $(ls -t results/benchmark_*.csv | grep -v processed | head -1)
```

Скрипт выведет:
- Детальную таблицу результатов
- Статистику производительности
- Анализ латентности и пропускной способности
- Сохранит обработанные данные

### 5. Построение графиков (Generate graphs)

```bash
python3 analysis/plot_graphs.py
```

Графики будут сохранены в `graphs/`:
- `time_vs_message_size.png` - время vs размер сообщения
- `bandwidth_vs_message_size.png` - пропускная способность vs размер
- `latency_analysis.png` - анализ латентности
- `combined_time_bandwidth.png` - комбинированный график
- `performance_heatmap.png` - тепловая карта
- `summary_table.txt` - сводная таблица

## Структура результатов (Results Structure)

```
results/
├── benchmark_YYYYMMDD_HHMMSS.csv          # Сырые данные
├── benchmark_YYYYMMDD_HHMMSS_processed.csv # Обработанные данные
└── benchmark_YYYYMMDD_HHMMSS_summary.txt   # Краткая сводка

graphs/
├── time_vs_message_size.png
├── bandwidth_vs_message_size.png
├── latency_analysis.png
├── combined_time_bandwidth.png
├── performance_heatmap.png
└── summary_table.txt
```

## Параметры программы (Program Parameters)

```bash
mpirun -np 2 bin/message_exchange <message_size> <iterations> [output_file]
```

**ВАЖНО:** Программа требует ровно 2 MPI процесса (`-np 2`)!

- `message_size` - размер сообщения в байтах
- `iterations` - количество обменов для усреднения
- `output_file` - (опционально) файл для сохранения результатов

## Примеры использования (Usage Examples)

```bash
# Базовый тест
mpirun -np 2 bin/message_exchange 1024 100

# Тест с разными размерами
mpirun -np 2 bin/message_exchange 1 100        # 1 byte
mpirun -np 2 bin/message_exchange 1024 100     # 1 KB
mpirun -np 2 bin/message_exchange 1048576 50   # 1 MB
mpirun -np 2 bin/message_exchange 16777216 20  # 16 MB

# С сохранением в файл
mpirun -np 2 bin/message_exchange 1024 100 results/my_test.csv

# Серия тестов
for size in 1024 10240 102400 1048576; do
    mpirun -np 2 bin/message_exchange $size 100 results/series.csv
done
```

## Требования (Requirements)

### Система (System)
- macOS или Linux
- MPI реализация (OpenMPI или MPICH)
- Python 3.6+

### Python библиотеки (Python packages)
```bash
pip3 install pandas matplotlib numpy
```

## Установка зависимостей (Install Dependencies)

### macOS
```bash
# Установка OpenMPI
brew install open-mpi

# Python пакеты
pip3 install pandas matplotlib numpy
```

### Linux (Ubuntu/Debian)
```bash
# Установка OpenMPI
sudo apt-get update
sudo apt-get install libopenmpi-dev openmpi-bin

# Python пакеты
pip3 install pandas matplotlib numpy
```

## Устранение проблем (Troubleshooting)

### Ошибка: MPI compiler not found

```bash
# macOS
brew install open-mpi

# Linux
sudo apt-get install libopenmpi-dev
```

### Ошибка: This program requires exactly 2 MPI processes

Убедитесь, что используете `-np 2`:
```bash
mpirun -np 2 bin/message_exchange 1024 100
```

### Python библиотеки не найдены

```bash
pip3 install --user pandas matplotlib numpy
```

### Программа работает медленно

- Уменьшите количество итераций
- Уменьшите максимальный размер сообщения
- Используйте меньше размеров в бенчмарке

## Что дальше? (Next Steps)

1. Изучите сгенерированные графики в `graphs/`
2. Проанализируйте `summary_table.txt`
3. Обратите внимание на:
   - Зависимость времени от размера сообщения
   - Пиковую пропускную способность
   - Латентность для малых сообщений
   - Точку перехода от латентности к пропускной способности

## Для отчета (For Report)

Используйте следующие графики:

1. **time_vs_message_size.png**
   - Показывает логарифмическую зависимость
   - Демонстрирует рост времени с размером

2. **bandwidth_vs_message_size.png**
   - Показывает достижение пиковой пропускной способности
   - Важен для анализа эффективности

3. **combined_time_bandwidth.png**
   - Комплексный анализ
   - Показывает обе метрики одновременно

4. **summary_table.txt**
   - Числовые данные для таблиц в отчете
   - Точные значения метрик

## Ключевые вопросы для анализа

1. Как время передачи зависит от размера сообщения?
2. Какова латентность системы (время для малых сообщений)?
3. Какова пиковая пропускная способность?
4. При каком размере сообщения достигается пиковая пропускная способность?
5. Как накладные расходы MPI влияют на производительность?

## Типичные результаты (Typical Results)

### Латентность (малые сообщения)
- **1 byte - 1 KB:** 0.001 - 0.1 ms
- Почти не зависит от размера

### Пропускная способность (большие сообщения)
- **Локальная машина:** 1000 - 10000 MB/s
- **Сеть Ethernet (1 Gbps):** 100 - 120 MB/s
- **Сеть Ethernet (10 Gbps):** 1000 - 1200 MB/s

### Переходная область
- **1 KB - 1 MB:** Переход от латентности к пропускной способности
- Растущая эффективность

## Полезные команды (Useful Commands)

```bash
# Просмотр последних результатов
ls -lt results/*.csv | head -5

# Быстрый анализ последнего бенчмарка
python3 analysis/analyze.py $(ls -t results/benchmark_*.csv | grep -v processed | head -1)

# Генерация всех графиков
python3 analysis/plot_graphs.py

# Просмотр сводной таблицы
cat graphs/summary_table.txt

# Очистка результатов
rm -rf results/*.csv graphs/*.png
```

## Проверка MPI (Check MPI)

```bash
# Проверка версии MPI
mpirun --version

# Проверка количества доступных процессов
mpirun -np 2 hostname

# Тест простой MPI программы
mpirun -np 2 echo "MPI is working"
```

---

**Удачи в исследовании производительности MPI! 🚀**